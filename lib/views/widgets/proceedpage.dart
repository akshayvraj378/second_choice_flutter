import 'package:flutter/material.dart';
class proceeds extends StatefulWidget {
  const proceeds({super.key});

  @override
  State<proceeds> createState() => _proceedsState();
}

class _proceedsState extends State<proceeds> {
  @override
  Widget build(BuildContext context) {
    return  Scaffold(appBar: AppBar(),
    body: Column(children: [Card()]),);
  }
}
